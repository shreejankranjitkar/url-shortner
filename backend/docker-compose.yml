services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: shrijan
      POSTGRES_PASSWORD: url_shortner_pass
      POSTGRES_DB: url_shortner
    
    ports:
      - 5432:5432
    
    volumes:
      - db_data:/var/lib/postgresql
volumes:
  db_data: